#include <stdio.h>
int main()
{
int a,b;
printf("Enter the value of a before swapping:");
scanf("%d",&a);
printf("Enter the value of b before swapping:");
scanf("%d",&b);
printf("The value of a after swapping is: %d",b);
printf("\nThe value of b after swapping is: %d",a);
return 0;
}
